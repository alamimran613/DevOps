# Create image from Dockerfile, -t means tag, you provide your dockerhub username/image name then location of dockerfile
docker build -t <your image name>:version .
docker build -t alamimran613/ubuntu:v1 .

