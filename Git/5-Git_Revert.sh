## Git Revert
git revert <commit id>
git revert 4acff5544

# Note: Git revert help us to get back second last commit file into latest commit. For example if we change anything in file and hit commit by accidently then we can use git revert command for undo last commit. Make sure to enter accidently commit id undo the change.

